/** @format */

import { LicenseeForm } from 'components';
/**
 * present the licensee form in create mode
 * @returns {React.jsx}
 */
export default function LicenseeFormCreate() {
	return <LicenseeForm editMode={true} />;
}
